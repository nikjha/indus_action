# Local Development Environment Configuration for Windows
# This file is used when running services locally without Docker

# Environment
ENVIRONMENT=development
DEBUG=true

# Database Configuration
DB_HOST=localhost
DB_PORT=5433
DB_USER=app
DB_PASSWORD=app#123#
DB_NAME=taskdb

# Redis Configuration (Cloud Redis)
REDIS_URL=redis://default:TKd5Nfb7390758iXfHTUo1qTZ0Pm4EeG@redis-15766.c330.asia-south1-1.gce.cloud.redislabs.com:15766

# Service Configuration
AUTH_SECRET=dev-secret-key-change-in-production
DOCS_USER=docs
DOCS_PASS=docs123

# API Gateway
API_GATEWAY_HOST=localhost
API_GATEWAY_PORT=8000

# Auth Service
AUTH_SERVICE_HOST=localhost
AUTH_SERVICE_PORT=8001

# User Service
USER_SERVICE_HOST=localhost
USER_SERVICE_PORT=8002

# Task Service
TASK_SERVICE_HOST=localhost
TASK_SERVICE_PORT=8003

# Eligibility Engine
ELIGIBILITY_ENGINE_HOST=localhost
ELIGIBILITY_ENGINE_PORT=8004

# Worker
WORKER_HOST=localhost
WORKER_PORT=5000

# Logging
LOG_LEVEL=INFO

# Feature Flags
USE_REDIS_CACHE=true
ENABLE_ASYNC_TASKS=true
